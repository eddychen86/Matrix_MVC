<aside id="friends_list" class="friend-panel z-[1000] flex h-[911px] flex-col rounded-xl shadow">
    <div class="friend-search-bar">
        <button type="button" class="friend-icon-btn" v-on:click="focusInput">
            <svg class="friend-icon" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                <path d="M0 0h24v24H0z" fill="none"></path>
                <path d="M12 12c2.21 0 4-1.79 4-4s-1.79-4-4-4-4 1.79-4 4 1.79 4 4 4zm0 2c-2.67 0-8 1.34-8 4v2h16v-2c0-2.66-5.33-4-8-4z"></path>
            </svg>
        </button>
        <input type="text" placeholder="Search friends" class="friend-search-input" v-model="searchFriends" ref="myInput" />
    </div>

    <div class="flex-1 overflow-y-auto">
        @foreach (var item in ViewBag.Hotlist)
        {
            <div class="friend-item flex items-center gap-2 px-4 py-2 transition hover:bg-gray-800">
                <img src="@(item.Author?.AvatarPath ?? " /img/avatar_default.png")" class="friend-avatar h-8 w-8 rounded-full object-cover" />
                <span class="friend-name">@item.Author?.DisplayName</span>
            </div>
        }
    </div>
</aside>

<script src="https://unpkg.com/vue@3/dist/vue.global.prod.js"></script>
<script>


    const app = Vue.createApp({
                data() {
                    return {
                        searchFriends:""
                    }
                },
                computed:{
                    filterFriends(){
                        return this.
                    }
                },
                methods: {
                    focusInput(){
                        this.$refs.myInput.focus();
                    }

                }
            });app.mount('#friends_list');

</script>
